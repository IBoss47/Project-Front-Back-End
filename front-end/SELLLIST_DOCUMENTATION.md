# 📚 SellListPage Documentation

## Overview
หน้า **SellListPage** เป็นหน้าแสดงรายการสรุปวิชาทั้งหมด พร้อมระบบกรอง, ค้นหา, เรียงลำดับ และเปลี่ยนมุมมอง

---

## 🗂️ File Structure

```
src/
├── pages/
│   └── SellListPage.jsx       # หน้าหลักที่รวม components ทั้งหมด
│
└── components/
    ├── FilterSidebar.jsx      # ตัวกรองด้านซ้าย (ชั้นปี, วิชา, ราคา)
    ├── SearchBar.jsx          # ช่องค้นหา
    ├── SaleList.jsx           # BookCard แสดงข้อมูลสรุปแต่ละชุด
    ├── BookDetailModal.jsx    # Modal แสดงรายละเอียดเมื่อคลิกการ์ด
    └── LoadingSpinner.jsx     # แสดงขณะโหลดข้อมูล
```

---

## 🔄 Data Flow Diagram

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           SellListPage                                   │
│                                                                          │
│  ┌─────────────┐                                                        │
│  │   API Call  │  GET /notes                                            │
│  │  fetchBooks │────────────────┐                                       │
│  └─────────────┘                │                                       │
│                                 ▼                                       │
│                        ┌──────────────┐                                 │
│                        │    books     │  (ข้อมูลดิบจาก API)              │
│                        │    state     │                                 │
│                        └──────┬───────┘                                 │
│                               │                                         │
│  ┌────────────────────────────┼────────────────────────────────┐       │
│  │            User Interactions (Filter/Search)                 │       │
│  │                                                              │       │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │       │
│  │  │FilterSidebar │  │  SearchBar   │  │SemesterFilter│       │       │
│  │  │              │  │              │  │  (Dropdown)  │       │       │
│  │  │ • ชั้นปี      │  │ • ค้นหาชื่อ   │  │ • กลางภาค   │       │       │
│  │  │ • วิชา       │  │ • รหัสวิชา   │  │ • ปลายภาค   │       │       │
│  │  │ • ราคา       │  │              │  │              │       │       │
│  │  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘       │       │
│  │         │                 │                 │                │       │
│  └─────────┼─────────────────┼─────────────────┼────────────────┘       │
│            │                 │                 │                        │
│            ▼                 ▼                 ▼                        │
│     ┌──────────────────────────────────────────────┐                   │
│     │              useEffect Trigger               │                   │
│     │  dependencies: [selectedSemester, selectedYear,                  │
│     │   selectedCourseId, priceRange, searchTerm, books]               │
│     └────────────────────┬─────────────────────────┘                   │
│                          │                                              │
│                          ▼                                              │
│                 ┌────────────────┐                                      │
│                 │  applyFilters()│  กรองข้อมูลตามเงื่อนไข                 │
│                 └────────┬───────┘                                      │
│                          │                                              │
│                          ▼                                              │
│                 ┌────────────────┐                                      │
│                 │ filteredBooks  │  (ข้อมูลหลังกรอง)                     │
│                 │     state      │                                      │
│                 └────────┬───────┘                                      │
│                          │                                              │
│    ┌─────────────────────┼─────────────────────┐                       │
│    │                     │                     │                        │
│    ▼                     ▼                     ▼                        │
│ ┌──────────┐      ┌──────────────┐      ┌──────────────┐               │
│ │  Sort    │      │  Pagination  │      │  ViewMode    │               │
│ │ Dropdown │      │              │      │   Toggle     │               │
│ │          │      │ currentPage  │      │              │               │
│ │ • ใหม่ล่าสุด│      │ booksPerPage │      │ • Grid View │               │
│ │ • ราคาต่ำ  │      │ = 12         │      │ • List View │               │
│ │ • ราคาสูง │      │              │      │              │               │
│ │ • ยอดนิยม │      │              │      │              │               │
│ └────┬─────┘      └──────┬───────┘      └──────┬───────┘               │
│      │                   │                     │                        │
│      └───────────────────┼─────────────────────┘                        │
│                          │                                              │
│                          ▼                                              │
│                 ┌────────────────┐                                      │
│                 │  currentBooks  │  (ข้อมูลที่จะแสดงในหน้าปัจจุบัน)        │
│                 └────────┬───────┘                                      │
│                          │                                              │
│                          ▼                                              │
│           ┌──────────────────────────────┐                             │
│           │      Books Grid / List       │                             │
│           │                              │                             │
│           │  ┌────────┐  ┌────────┐     │                             │
│           │  │BookCard│  │BookCard│ ... │                             │
│           │  │(SaleList)│ │(SaleList)│   │                             │
│           │  └────────┘  └────────┘     │                             │
│           └──────────────────────────────┘                             │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 📦 Component Details

### 1. SellListPage.jsx (Main Page)

#### States

| State | Type | Default | Description |
|-------|------|---------|-------------|
| `books` | Array | `[]` | ข้อมูลดิบจาก API |
| `filteredBooks` | Array | `[]` | ข้อมูลหลังผ่านการกรอง |
| `sortBy` | String | `'newest'` | วิธีเรียงลำดับ |
| `selectedSemester` | String | `'all'` | กรองตามภาคเรียน (กลาง/ปลาย) |
| `selectedYear` | String | `'all'` | กรองตามชั้นปี |
| `selectedCourseId` | String | `'all'` | กรองตามวิชา |
| `priceRange` | Object | `{min:0, max:1000}` | ช่วงราคา |
| `searchTerm` | String | `''` | คำค้นหา |
| `loading` | Boolean | `true` | สถานะกำลังโหลด |
| `currentPage` | Number | `1` | หน้าปัจจุบัน (pagination) |
| `viewMode` | String | `'grid'` | โหมดแสดงผล (grid/list) |
| `showFilters` | Boolean | `false` | แสดง/ซ่อน filter บน mobile |

#### Key Functions

```javascript
// ดึงข้อมูลจาก API
fetchBooks()          → GET /notes → setBooks() → setFilteredBooks()

// กรองข้อมูล (trigger โดย useEffect)
applyFilters()        → filter by semester, year, course, price, search

// Handlers สำหรับ filter
handleSearch(term)              → setSearchTerm()
handleSemesterFilter(semester)  → setSelectedSemester()
handleYearFilter(year)          → setSelectedYear()
handleCourseFilter(courseId)    → setSelectedCourseId()
handlePriceRangeChange(min,max) → setPriceRange()

// เรียงลำดับ
handleSort(sortValue)  → sort filteredBooks by price/date/reviews

// Pagination
paginate(pageNumber)   → setCurrentPage()
```

---

### 2. FilterSidebar.jsx

#### Props

| Prop | Type | Description |
|------|------|-------------|
| `onSemesterChange` | Function | Callback เมื่อเปลี่ยนภาคเรียน |
| `onYearChange` | Function | Callback เมื่อเปลี่ยนชั้นปี |
| `onCourseChange` | Function | Callback เมื่อเปลี่ยนวิชา |
| `onPriceRangeChange` | Function | Callback เมื่อเปลี่ยนช่วงราคา |
| `selectedSemester` | String | ค่าภาคเรียนปัจจุบัน |
| `selectedYear` | String | ค่าชั้นปีปัจจุบัน |
| `selectedCourseId` | String | ค่าวิชาปัจจุบัน |

#### Logic Flow

```
เลือกชั้นปี
    │
    ▼
useEffect trigger
    │
    ▼
getCoursesByYear(selectedYear)
    │
    ▼
setAvailableCourses()  → แสดงเฉพาะวิชาของปีที่เลือก
```

---

### 3. SearchBar.jsx

#### Props

| Prop | Type | Description |
|------|------|-------------|
| `onSearch` | Function | Callback เมื่อ submit ค้นหา |
| `placeholder` | String | ข้อความ placeholder |

#### Flow

```
พิมพ์ในช่อง input
    │
    ▼
setSearchTerm(e.target.value)
    │
    ▼
กด Enter / คลิกปุ่มค้นหา
    │
    ▼
handleSubmit() → onSearch(searchTerm)
    │
    ▼
Parent: handleSearch() → setSearchTerm() → trigger useEffect → applyFilters()
```

---

### 4. SaleList.jsx (BookCard)

#### Props

| Prop | Type | Description |
|------|------|-------------|
| `book` | Object | ข้อมูลสรุปวิชา |

#### Book Object Structure

```javascript
{
  id: Number,
  book_title: String,        // ชื่อสรุป
  description: String,       // รายละเอียด
  price: Number,             // ราคา
  cover_image: String,       // URL รูปปก
  exam_term: String,         // ประเภท (กลางภาค/ปลายภาค)
  status: String,            // สถานะ (available/sold/reserved)
  reviews: Number,           // จำนวนรีวิว
  condition: String,         // ประเภทเนื้อหา
  created_at: String,        // วันที่สร้าง
  course: {                  // ข้อมูลวิชา
    id: Number,
    code: String,            // รหัสวิชา
    name: String,            // ชื่อวิชา
    year: String             // ชั้นปี
  },
  seller: {                  // ข้อมูลผู้ขาย
    id: Number,
    fullname: String
  }
}
```

#### Features
- แสดง cover image หรือ placeholder
- Badge แสดงสถานะ (พร้อมขาย/ขายแล้ว/จองแล้ว)
- Badge แสดงประเภทสอบ และชั้นปี
- คลิกเปิด `BookDetailModal`

---

## 🎨 UI Layout

```
┌─────────────────────────────────────────────────────────────────┐
│                         Header                                   │
│                 📝 ค้นหาสรุปวิชาเรียน                              │
└─────────────────────────────────────────────────────────────────┘

┌──────────────┬──────────────────────────────────────────────────┐
│              │                                                   │
│   Filter     │  ┌─────────────────────────────────────────────┐ │
│   Sidebar    │  │ SearchBar │ SemesterFilter │ Sort │ ViewMode│ │
│              │  └─────────────────────────────────────────────┘ │
│  ┌────────┐  │                                                   │
│  │ ชั้นปี  │  │  พบสรุปวิชา XX ชุด                                │
│  └────────┘  │                                                   │
│  ┌────────┐  │  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐    │
│  │  วิชา  │  │  │ Card 1 │ │ Card 2 │ │ Card 3 │ │ Card 4 │    │
│  └────────┘  │  └────────┘ └────────┘ └────────┘ └────────┘    │
│  ┌────────┐  │                                                   │
│  │  ราคา  │  │  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐    │
│  └────────┘  │  │ Card 5 │ │ Card 6 │ │ Card 7 │ │ Card 8 │    │
│              │  └────────┘ └────────┘ └────────┘ └────────┘    │
│              │                                                   │
│              │             ← 1 2 3 4 5 →                         │
│              │               Pagination                          │
└──────────────┴──────────────────────────────────────────────────┘
```

---

## 🔧 Filter Logic (applyFilters)

```javascript
let filtered = [...books];

// 1. กรองตามภาคเรียน
if (selectedSemester !== 'all') {
  filtered = filtered.filter(book => book.exam_term === selectedSemester);
}

// 2. กรองตามชั้นปี
if (selectedYear !== 'all') {
  filtered = filtered.filter(book => book.course?.year === selectedYear);
}

// 3. กรองตามวิชา
if (selectedCourseId !== 'all') {
  filtered = filtered.filter(book => book.course?.id === parseInt(selectedCourseId));
}

// 4. กรองตามช่วงราคา
filtered = filtered.filter(book => 
  book.price >= priceRange.min && book.price <= priceRange.max
);

// 5. กรองตามคำค้นหา
if (searchTerm) {
  filtered = filtered.filter(book => 
    book.book_title?.toLowerCase().includes(searchTerm.toLowerCase()) ||
    book.description?.toLowerCase().includes(searchTerm.toLowerCase()) ||
    book.course?.name?.toLowerCase().includes(searchTerm.toLowerCase()) ||
    book.course?.code?.toLowerCase().includes(searchTerm.toLowerCase())
  );
}
```

---

## 📱 Responsive Design

| Screen Size | Behavior |
|-------------|----------|
| **Desktop (lg+)** | FilterSidebar แสดงด้านซ้าย, Grid 4-5 columns |
| **Tablet (md)** | FilterSidebar ซ่อน, มีปุ่ม toggle, Grid 2-3 columns |
| **Mobile (sm)** | FilterSidebar ซ่อน, มีปุ่ม toggle, Grid 1 column |

---

## 🚀 API Endpoint

```
GET /notes

Response:
[
  {
    "id": 1,
    "book_title": "สรุป Calculus I",
    "price": 50,
    "exam_term": "กลางภาค",
    "course": {
      "id": 1,
      "code": "MA101",
      "name": "แคลคูลัส 1",
      "year": "ปี 1"
    },
    "seller": {
      "id": 1,
      "fullname": "John Doe"
    }
    ...
  }
]
```

---

## 📝 Quick Reference

### เพิ่ม Filter ใหม่

1. เพิ่ม state ใน `SellListPage.jsx`
2. เพิ่ม handler function
3. เพิ่มใน dependencies ของ `useEffect`
4. เพิ่มเงื่อนไขใน `applyFilters()`
5. ส่ง props ไป `FilterSidebar` หรือสร้าง dropdown ใหม่

### เปลี่ยน Sort Options

แก้ไขที่ `handleSort()` ใน `SellListPage.jsx`:

```javascript
case 'your-new-sort':
  sorted.sort((a, b) => /* your logic */);
  break;
```
